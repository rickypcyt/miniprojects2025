// C program that shows a desktop notification when it's time to go to the gym to eat to turn off
// the screen and sleep.

#include <stdio.h>
#include <stdlib.h>

char mensaje = hola;

int main() {
    printf("");
    if (mensaje == NULL) {
        printf("No hay mensaje que printear\n");
    }
}
